package main
import "fmt"

func main() { 
	var (
		maximal, minimal int
		
	)
	data := [...]int {1, 2, 3, 4, 5}
	maximal := [0]
	minimal := [0]
	for i := 0; i <= len(data); i++ {
		if maximal <= len(data) 
	}
		

}